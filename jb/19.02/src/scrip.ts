function boolean(isOdd: boolean): string{
 return (`is the number odd? ${isOdd}`)
}

boolean(true)

function number(random: number): string{
     return `the chosen number is: ${random}`
    }
    
    number(12)


    function string(random: string): string{
        return `the chosen word is: ${random}`
       }
       
       string("asasa")